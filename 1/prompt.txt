So select doesn't work on DG data that is not interpolated, but I'd like to add some support for this. Indexing those dimensions should work, as this doesn't need interpolation at all. When passed floating point numbers (e.g. 0.0), it should snap to the nearest neighbor to avoid interpolation. Are there any other cases that are needed. Ask questions as needed. So say we load a 6 coefficient 3D array. Then the data is like (c0,c1,c2,c3,c4,c5,c6) on coodrinates
  (x,y,z). Select should select, for instance, y=0.5. So we are looking at a grid (x,y=0.5,z). This select operation should snap to the data
  which is closest to 0.5, colapsing that dimension. This doesn't require any interpolation. Similarly, selecting index 5 in the y direction
  should not require interpolation. Similarly, when selecting interpolated data, it becomes similarly multicoeffieicnted arrays, but selecting operates on the grid to select other parts of the grid. We don't want to make up data that is not there. This is a tool for exact inspection of data

---

> Raw DG coefficients describe a polynomial across the whole cell, including y. Copying them unchanged can narrow y to one cell, but cannot turn them into a valid lower-dimensional DG field. Should select preserve the coefficients exactly and retain a length-1 y axis, or evaluate the polynomial at the selected cell’s center and remove y (which changes the coefficients)?

Preserve coefficients; retain length-1 axis (Recommended)